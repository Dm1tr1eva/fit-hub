name: Supabase keep-alive

on:
  schedule:
    - cron: "37 14 * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keepalive endpoint
        run: curl -sf https://fit-hub-daryna1.vercel.app/api/keepalive
